package com.xlythe.hex;

/** Runs every screen golden at the narrowest supported landscape phone width. */
public final class CompactLandscapeScreenshotTest extends ScreenshotTest {
    public CompactLandscapeScreenshotTest() {
        super(TestDeviceConfigs.COMPACT_LANDSCAPE);
    }
}
